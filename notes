run as an app without shell
highlight only the current line
fix the mouse scroll
highlight error, and jump to next error
better keybindings
query replace go to prev?
find a way you can edit without mouse at all